<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Beanify.Views.OrderReviewView"
             xmlns:b="clr-namespace:Xamarin.Forms.BehaviorsPack;assembly=Xamarin.Forms.BehaviorsPack"
             BackgroundColor="AliceBlue"
             BindingContext="{Binding OrderReviewViewModel, Source={StaticResource Locator}}">
    <ContentPage.Behaviors>
        <b:EventToCommandBehavior EventName="SizeChanged" Command="{Binding Commands[SizeChanged]}" />
    </ContentPage.Behaviors>

    <ContentPage.Content>
        <StackLayout Orientation="{Binding ScreenOrientation,Mode=TwoWay}"  >
            <StackLayout  Margin="15, 15, 15, 0" VerticalOptions="StartAndExpand" HorizontalOptions="Center" HeightRequest="250" WidthRequest="350">
                <Label Text="Order Review" FontSize="25" HorizontalOptions="Center" VerticalOptions="Center"/>
                <Label Text="{Binding Order.ProductName}" FontSize="Large" x:Name="title" HorizontalOptions="Center" VerticalOptions="Center"/>
                <Image Source="{Binding Order.ImagePath}" Aspect="AspectFill" />
                
            </StackLayout>
            <StackLayout Margin="15, 15, 15, 0"  VerticalOptions="FillAndExpand"  HorizontalOptions="FillAndExpand">
                <StackLayout  Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                    <Label Text="{Binding Order.ProductName}" FontSize="Large" HorizontalOptions="StartAndExpand" LineBreakMode="WordWrap"/>
                    <Label Text="{Binding Order.Quantity}" FontSize="Large" HorizontalOptions="End" LineBreakMode="WordWrap"/>
                </StackLayout>
                <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                    <Label Text="Item Name" HorizontalOptions="StartAndExpand" LineBreakMode="WordWrap"/>
                    <Label Text="Quantity" HorizontalOptions="End" LineBreakMode="WordWrap"/>
                </StackLayout>
                <StackLayout Orientation="Vertical" VerticalOptions="StartAndExpand" >
                    <Label Text="Price :" HorizontalOptions="CenterAndExpand"/>
                    <Label Text="{Binding Order.Price}" FontSize="50" FontAttributes="Italic" HorizontalOptions="Fill"/>
                </StackLayout>
                <Button Text="Confirm and Order" Command="{Binding Commands[Order]}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>